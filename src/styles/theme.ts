import { ThemeOptions } from '@material-ui/core/styles/createMuiTheme'

const THEME: Readonly<ThemeOptions> = {
  palette: {
    primary: {
      main: 'rgb(0,122,255)',
    },
  },
}

export { THEME }
